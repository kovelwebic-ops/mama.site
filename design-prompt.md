# Промпт для редизайну: сайт-каталог кондитерки «Słodkie Marzenia»

> Цей файл самодостатній. Він написаний так, щоб виконавець із чистим контекстом
> міг зробити весь редизайн, не читаючи попереднє листування.

---

## 1. Що це за проєкт

Сайт-каталог домашньої кондитерки. Приватний майстер (мама замовника), працює в Польщі,
ціни в злотих. Сайт **не магазин**: немає кошика, оплати, доставки й особистого кабінету.
Задача сайту — красиво показати асортимент і дати людині написати/подзвонити.

**Робоча папка:** `C:\Users\user\Desktop\Mom site`

**Що вже є в папці:**

| Папка | Вміст |
|---|---|
| `Cakes/` | 21 фото тортів + `Data.txt` |
| `Cheesecakes/` | 5 фото чізкейків + `Data.txt` |
| `Мусові/` | 6 фото мусових бенто + `Data.txt` |
| `Zefiry/` | 27 фото зефіру + `Data.txt` |
| `Кендібар/` | 8 фото трайфлів/капкейків + `Data.txt` |

`Data.txt` у кожній папці — джерело правди по тексту. Формат запису:

```
Назва: Апельсиновий
Фото: Апельсиновий.jpg
Склад: Бісквіт з апельсиновою цедрою, легкий шоколадний крем, ...
Ціна: 110 zł/кг
```

Записи розділені рядком `---`. Порожнє значення після двокрапки = даних ще немає.
Деякі записи мають ще поле `Вага:`.

**У корені лежить старий MVP-сайт** (`index.html`, `cakes.html`, `cheesecakes.html`,
`mousse.html`, `zefir.html`, `candybar.html`, `style.css`). Це був чорновий тест,
щоб зрозуміти яких даних бракує. **Його треба повністю викинути й замінити.**
Нічого з його верстки не переносити.

---

## 2. Головна ідея

Референс — Shopify-тема Flowbit для одягових брендів (flowbit.media).
Замовнику подобається саме **сторінка товару** цієї теми і загальна кількість повітря.

Ключове, що треба перенести:

- **Товар «плаває» на фоні.** Фото зняті на чистому білому, фон сайту теж чистий білий →
  жодних карток, рамок, тіней, заокруглень навколо фото. Продукт просто лежить на сторінці.
- **Різкий контраст масштабів.** Гігантський жирний UPPERCASE-заголовок поруч із дуже
  дрібним текстом. Між ними нічого середнього.
- **Монохром.** Чорний текст, чорна кнопка, білий фон. Єдиний колір на сторінці — сам десерт.
- **Багато повітря.** Це головне враження від референсу. Не заповнювати порожнечу.

> На відміну від референсу фон **чисто білий `#FFFFFF`**, а не сірий.
> Фото спеціально знімались на білому саме заради цього.

---

## 3. Технічні обмеження

- Тільки **HTML + CSS + JS**. Ніяких фреймворків, збірок, npm, бандлерів, генераторів.
- Сайт має відкриватись подвійним кліком по `index.html` (протокол `file://`) і працювати.
- Шрифти — Google Fonts (підключення `<link>`), решта ассетів локальна.
- **Адаптив обов'язковий.** Основний трафік прийде з Instagram, тобто з телефону.
  Мобільний макет — повноцінний, а не «щоб не поламалось».
- Без анімацій заради анімацій. Рух дозволений лише там, де він описаний нижче.

---

## 4. Дизайн-система

### 4.1 Кольори

| Токен | Значення | Де |
|---|---|---|
| `--bg` | `#FFFFFF` | фон усього сайту |
| `--ink` | `#0A0A0A` | заголовки, основний текст, кнопка |
| `--muted` | `#6B6B6B` | ціни в сітці, підписи, другорядне |
| `--line` | `#E5E5E5` | волосяні лінії, рамки пігулок |
| `--ink-hover` | `#2A2A2A` | ховер чорної кнопки |

Більше кольорів немає. Ніяких акцентів, градієнтів, тіней.

### 4.2 Типографіка

Один сімейний шрифт — **Inter** (Google Fonts, підключити `400,500,700,900`,
набори `latin`, `latin-ext`, `cyrillic`).

> Inter обраний тому, що покриває **і українську кирилицю, і польські діакритики**
> (`ł ż ą ę ś ć ń ó ź`). Модні важкі гротески на кшталт Archivo Black кирилиці не мають —
> не використовувати.

| Роль | Розмір | Вага | Регістр | Трекінг | Інтерліньяж |
|---|---|---|---|---|---|
| Заголовок сторінки / hero | `clamp(48px, 8vw, 120px)` | 900 | UPPERCASE | `-0.03em` | `0.9` |
| Назва товару на його сторінці | `clamp(36px, 5vw, 72px)` | 900 | UPPERCASE | `-0.03em` | `0.92` |
| Заголовок секції | `clamp(28px, 4vw, 48px)` | 900 | UPPERCASE | `-0.02em` | `1.0` |
| Назва товару в сітці | `14px` | 500 | звичайний | `0` | `1.4` |
| Ціна в сітці | `14px` | 400 | звичайний | `0` | `1.4` |
| Ціна на сторінці товару | `32px` | 700 | звичайний | `-0.01em` | `1.2` |
| Список складу | `12px` | 500 | UPPERCASE | `0.02em` | `1.65` |
| Пункти меню, дрібні лінки | `12px` | 500 | UPPERCASE | `0.06em` | `1.2` |

### 4.3 Сітка й відступи

- Контейнер: `max-width: 1600px`, падінги `0 40px` (desktop), `0 20px` (mobile).
- Сітка каталогу: **5 колонок** desktop → 4 (≤1400px) → 3 (≤1024px) → 2 (≤640px).
- Гепи: `40px` по горизонталі, `72px` по вертикалі. Вертикальний геп більший за
  горизонтальний — це дає те саме відчуття повітря, що в референсі.
- Вертикальний ритм між секціями: `120px` desktop, `72px` mobile.
- Фото товару в сітці: квадрат `1:1`, `object-fit: contain`, фон білий, без рамки.

---

## 5. Компоненти

### 5.1 Шапка

Липка (`sticky`), висота 72px, фон білий. Нижня волосяна лінія `--line` з'являється
лише коли сторінку прокрутили (не на самому верху).

Три зони:

- **Зліва** — пункти категорій: `ТОРТИ · ЧІЗКЕЙКИ · МУСОВІ · ЗЕФІР · КЕНДІБАР`
- **По центру** — вордмарк `SŁODKIE MARZENIA` (900, uppercase, трекінг `-0.02em`).
  Логотипу поки немає, замовник зробить пізніше — верстати так, щоб текст легко
  замінився на `<img>` без переїзду сусідніх елементів.
- **Справа** — три елементи:
  1. **іконка пошуку** — відкриває оверлей пошуку (див. 5.6)
  2. **іконка контактів** — відкриває те саме вікно контактів, що й кнопка «Замовити»
  3. **перемикач мови `UA / PL`** — текстом, не іконкою

> У референсі третя іконка — кошик. Кошика в нас немає і бути не повинно.
> Його місце займає перемикач мови.

На мобільному: бургер зліва, вордмарк по центру, пошук справа. Решта — всередині меню.

### 5.2 Картка товару в сітці

Без рамки, без тіні, без заокруглень, без підкладки.

```
[ квадратне фото, contain, на білому ]
Назва товару            ← 14px / 500 / --ink
120 zł                  ← 14px / 400 / --muted
```

Ховер: якщо у товару є друге фото — плавна підміна на нього за `200ms`.
Якщо другого фото немає — нічого не робити (ніякого зуму й підйому).

### 5.3 Кнопка «Замовити»

Чорний прямокутник на всю ширину блоку покупки, білий текст, радіус `6px`,
висота `52px`, вага 600. Ховер — `--ink-hover`. Це єдина «важка» кнопка на сайті.

### 5.4 Пігулки вибору варіанта

Прямокутники з радіусом `6px`, рамка `1px solid --line`, висота `44px`,
текст `13px/500`. Розкладаються сіткою по 3 в ряд.

- звичайний: біле тло, рамка `--line`
- ховер: рамка `--ink`
- вибраний: рамка `--ink` товщиною `1.5px` + текст `600`
- недоступний: текст `--muted`, рамка світліша, перекреслення

### 5.5 Вікно контактів (модалка)

Відкривається з кнопки «Замовити» і з іконки контактів у шапці.
Затемнення фону `rgba(0,0,0,.4)`, біле вікно по центру, максимум `420px`,
радіус `8px`, падінги `32px`.

Вміст:
- заголовок `ЗАМОВИТИ` (900, uppercase)
- якщо відкрито зі сторінки товару — рядок «Товар: <назва + вибрані варіанти>»
- посилання списком, кожне окремим рядком зі стрілкою `→`:
  `Instagram →`, `Telegram →`, `Телефон →`
- закриття: хрестик, клік по затемненню, `Esc`

### 5.6 Оверлей пошуку

Повноекранний білий оверлей. Велике поле вводу без рамки (тільки нижня лінія),
плейсхолдер `Пошук`. Результати — та сама сітка карток, оновлюється з кожним
символом. Пошук по назві + складу, регістронезалежний, клієнтський (по масиву товарів).
Порожній запит показує назви категорій як швидкі переходи.

### 5.7 Рухомі стрічки (тільки на головній)

Дві горизонтальні стрічки квадратних фото, висота ~260px:
- верхня їде **вліво**, нижня — **вправо**
- рух безперервний і плавний, лінійний, повний цикл ~40s
- дублювати набір фото, щоб стик був непомітний
- **пауза при наведенні**
- поважати `prefers-reduced-motion: reduce` — тоді стрічка стоїть і скролиться пальцем

---

## 6. Сторінки

### 6.1 Головна

1. **Hero** — на всю висоту екрана. Одне велике фото десерту по центру на білому,
   під ним вордмарк або короткий рядок-дескриптор. Максимум повітря, мінімум тексту.
   Внизу — тонкий індикатор скролу.
2. **Дві рухомі стрічки** (див. 5.7).
3. **Блоки категорій** — 5 штук: Торти, Чізкейки, Мусові десерти, Зефір, Кендібар.
   Кожен блок: велике фото + гігантська uppercase-назва + лічильник позицій.
4. **«ВСІ ТОВАРИ»** — великий текстовий заклик на всю ширину, веде на сторінку каталогу.
5. **Футер.**

### 6.2 Каталог / категорія

- Гігантський uppercase-заголовок зліва (`ВСІ ТОВАРИ` або назва категорії).
- Рядок керування під заголовком:
  - зліва: чіпи категорій (на сторінці «Всі товари») або фільтр за типом
  - справа: лічильник `51 позиція`, сортування (`За назвою` / `Ціна ↑` / `Ціна ↓`),
    перемикач щільності сітки (4 / 6 колонок) — як у референсі
- Сітка карток.

### 6.3 Сторінка товару — головний екран сайту

Три колонки на десктопі:

```
┌──────────────┬───────────────────────┬──────────────┐
│ ← Назад      │                       │  120 zł      │
│              │                       │              │
│ НАЗВА        │      [ ФОТО ]         │  Смак        │
│ ТОВАРУ       │                       │  [пігулки]   │
│              │   ‹  ● ○ ○  ›         │              │
│ • СКЛАД      │                       │  [ЗАМОВИТИ]  │
│ • ПУНКТ      │                       │              │
│ • ПУНКТ      │                       │  Instagram → │
│              │                       │  Telegram  → │
│              │                       │  Телефон   → │
└──────────────┴───────────────────────┴──────────────┘
     ~22%              ~46%                  ~26%
```

- **Ліва колонка:** посилання `← Назад`, потім гігантська назва товару,
  потім склад як **булет-лист капсом**, дрібним, щільним (це прямо з референсу).
  Якщо є вага — окремим рядком під списком.
- **Центр:** фото на білому, без рамки. Якщо у товару кілька фото (варіанти) —
  галерея: стрілки `‹ ›` з боків, крапки-індикатор знизу, свайп на тачі,
  перетягування мишею. **Індикатор має бути видно одразу**, щоб людина зрозуміла,
  що є ще фото.
- **Права колонка:** ціна великим, далі блоки вибору варіантів (див. 7),
  далі чорна кнопка «Замовити», далі посилання на соцмережі зі стрілками.

**Нижче першого екрана:** секція `ІНШІ ТОВАРИ` — горизонтальна стрічка карток
з тієї ж категорії (прокрутка мишею/свайпом, не автоматична). Під нею — футер.
Така навігація має бути на **кожній** сторінці товару, щоб з сайту не було глухих кутів.

**Мобільний порядок блоків:** `← Назад` → назва → фото з галереєю → ціна →
варіанти → кнопка → склад → соцмережі → інші товари.

### 6.4 Футер

Мінімальний: вордмарк, категорії списком, контакти, перемикач мови.
Волосяна лінія зверху. Без «підпишіться на розсилку» й іншого шуму.

---

## 7. Структура каталогу і варіанти

Усього **51 публічна позиція**. Частина товарів зводиться в одну сторінку
з перемикачем — саме так, як розміри куртки в референсі.

### 7.1 Торти — 21 позиція, без варіантів

Ціна вказана за кілограм (`80–130 zł/кг`). Кожен торт — окрема сторінка.
Дані брати з `Cakes/Data.txt`.

### 7.2 Чізкейки — 5 позицій, з варіантами

Блок **«Вага»** на кожній сторінці:

| Варіант | Ціна |
|---|---|
| 1,2 кг | 120 zł (Полуниця-лайм, Вишня-шоколад) / 130 zł (Фісташка-малина, Снікерс, Лохина-мигдаль) |
| 600 г | 80 zł — для всіх смаків |

**Вибір ваги змінює ціну на сторінці.**

Додатково у чізкейка **«Вишня-шоколад»** блок **«Шоколад»**: `Молочний` / `Темний`.

### 7.3 Мусові десерти — 6 позицій, без варіантів

Усі по `85 zł`, вага `460 г`. Бенто-тортики, покриті велюром.
Обов'язкова примітка на сторінці: **ціна вказана без декору**,
вага 450–470 г залежно від форми.

### 7.4 Зефір — 15 сторінок

**а) «Зефір поштучно» — одна сторінка, 9 смаків у перемикачі**

Блок **«Смак»**: `Класичний (яблучний)` · `Вишня` · `Полуниця` · `Бурувка` ·
`Банан` · `Персик` · `Абрикос` · `Кава` · `Слива`

Вибір смаку **міняє фото** (у кожного смаку своє фото) і назву.
Ціна й вага однакові для всіх: `8 zł/шт`, `60 г`.
Склад спільний: `Цукор, вода, інвертний сироп, фруктове/ягідне пюре, агар агар`.
Мінімального замовлення немає.

**б) «Морозиво зефір» — одна сторінка, два блоки вибору**

- Блок 1 — **«Шоколад»**: `Молочний` / `Білий` / `Темний`. Міняє фото (є 3 фото).
- Блок 2 — **«Смак зефіру»**: ті самі 9 смаків. Фото не міняє, це просто
  фіксація вибору — він підставляється в текст замовлення в модалці.

Ціна `10 zł/шт`, вага `60 г/шт`, **замовлення від 8 шт**.

**в) Решта 13 позицій — окремі сторінки без варіантів**

| Позиція | Ціна |
|---|---|
| Букет зефіру 20 см | 120 zł |
| Букет зефіру 25 см | 150 zł |
| Дитячий букет 16 см | 75 zł |
| Зефірне печиво | 20 zł/шт, мін. замовлення 5 шт |
| Сумочка з зефіром дитяча 13×13 см | 60 zł |
| Коробочка зефіру 12×15 см | 70 zł |
| Коробочка зефіру 16 см | 80 zł |
| Коробочка «Дитяча» тематична | 100–150 zł залежно від діаметра |
| Квіти на коробці 12 см | 100 zł |
| Коробочка на 8 завитків | 60 zł |
| Коробочка подвійна 16 см | 90 zł |
| Коробочка подвійна 18 см | 110 zł |
| Подвійна коробочка 15×15 см | 70 zł |

### 7.5 Кендібар — 4 позиції

Трайфли в стаканчиках. Склад повторює однойменні торти:

| Позиція | Фото | Склад |
|---|---|---|
| Шпинат полуниця | `Шпинат полуниця.jpg` | Шпинатний бісквіт, м'ятний мус, полуничне конфі, крем чіз маскарпоне |
| Шпинат малина | `Шпинат малина.jpg` | Шпинатний бісквіт, м'ятний мус, малинове конфі, крем чіз маскарпоне |
| Червоний оксамит | `Червоний оксамит.jpg` | Бісквіт червоний оксамит, вишневе конфі, полуничний ганаш, крем чіз, маскарпоне |
| Снікерс | `Снікерс.jpg` | Шоколадний бісквіт, солона карамель, арахіс, крем чіз |

**Цін і ваги для трайфлів поки немає** — див. розділ 9.

---

## 8. Дві мови (UA / PL)

- Перемикач `UA / PL` у правій частині шапки й у футері.
- Вибір зберігається в `localStorage`, застосовується одразу без перезавантаження.
- Механіка: словник у JS + атрибути `data-i18n` на елементах інтерфейсу.
  Назви й склади товарів беруться з полів `name_ua` / `name_pl`, `desc_ua` / `desc_pl`
  у даних товару.
- **Українські тексти є, польських ще немає.** Зробити механізм повністю робочим,
  польські значення залишити порожніми — тоді підставляється українське.
  Не вигадувати польські кулінарні терміни навмання: їх допише замовник.
- Ціни в `zł` в обох мовах, формат числа не міняється.

---

## 9. Що робити з неповними даними

На публічному сайті **не можна** показувати «ціна відсутня» червоним — це для
внутрішнього чернетки. Правила:

- **Немає ціни, але є назва і склад** (4 трайфли Кендібару) → показувати товар,
  замість ціни писати **«Ціна за запитом»** / `Cena na zapytanie`.
- **Немає взагалі нічого, крім фото** → **не показувати на сайті зовсім**.
  Це стосується `Zefiry/Коробочка 5.jpg`, `Zefiry/Коробочка 6.jpg`,
  `Кендібар/1.jpg`, `3.jpg`, `7.jpg`, `8.jpg`.
  Фото не видаляти — вони знадобляться, коли дані з'являться.

---

## 10. Як організувати файли

Рекомендований підхід (лишається чистим HTML/CSS/JS, без збірки):

```
index.html          головна
catalog.html        всі товари + категорії через ?cat=
product.html        шаблон сторінки товару, конкретний товар через ?id=
style.css           усі стилі
products.js         масив усіх товарів (id, категорія, фото, ціни, варіанти, тексти)
i18n.js             словник інтерфейсу UA/PL
app.js             рендер, галерея, варіанти, пошук, модалка, перемикач мови
```

Чому так, а не окремий HTML-файл на кожен товар: позицій 51, і всі сторінки товару
однакові за структурою. Один шаблон + масив даних — це менше файлів, ніж 51 копія,
і при зміні ціни правиться один рядок. Запити `?id=` коректно читаються через
`location.search` навіть при відкритті з `file://`.

> Якщо замовник захоче окремий фізичний файл на кожен товар — структура сторінки
> й дизайн від цього не міняються, зміниться лише спосіб зберігання даних.

---

## 11. Чого ще немає — заповнити перед публікацією

Ці значення в дизайні мають бути **видимими плейсхолдерами**, а не вигаданими:

1. **Контакти** — нік Instagram, нік/номер Telegram, номер телефону.
   Потрібні для модалки, футера й іконки контактів.
2. **Логотип** — поки текстовий вордмарк `SŁODKIE MARZENIA`.
   Перевірити написання назви польською у замовника.
3. **Ціни й вага трайфлів Кендібару** — 4 позиції.
4. **Дані для** `Zefiry/Коробочка 5.jpg`, `Zefiry/Коробочка 6.jpg`,
   `Кендібар/1.jpg`, `3.jpg`, `7.jpg`, `8.jpg`.
5. **Польські переклади** назв і складів.
6. **Фото для hero головної** — вибрати найсильніший кадр з наявних.

Непідтверджене припущення, яке варто перевірити: подвійні коробочки зефіру
`Подвійна коробка 2.jpg` (18 см / 110 zł) і `Подвійна коробка 3.jpg` (15×15 см / 70 zł)
зіставлені з цінами за фото, а не зі слів замовника.

---

## 12. Чекліст готовності

- [ ] Фон чисто білий, фото «плавають» без карток, рамок і тіней
- [ ] Гігантські uppercase-заголовки поруч із дрібним текстом
- [ ] Тільки чорний, білий і сірий — колір дає лише сам десерт
- [ ] Шапка: категорії зліва, вордмарк по центру, пошук + контакти + `UA/PL` справа
- [ ] Кошика немає ніде
- [ ] Головна: hero → дві стрічки назустріч → 5 категорій → «Всі товари» → футер
- [ ] Сторінка товару в 3 колонки, склад — булет-лист капсом зліва
- [ ] Галерея з видимим індикатором, свайп на тачі
- [ ] Зефір — одна сторінка з 9 смаками, фото міняється
- [ ] Морозиво зефір — два блоки вибору
- [ ] Чізкейки — вибір ваги міняє ціну
- [ ] Кнопка «Замовити» відкриває вікно з контактами і назвою обраного товару
- [ ] Знизу кожної сторінки товару — «Інші товари», глухих кутів немає
- [ ] Перемикач UA/PL працює, вибір запам'ятовується
- [ ] Товари без даних приховані, товари без ціни — «Ціна за запитом»
- [ ] Мобільний макет повноцінний
- [ ] Відкривається подвійним кліком по `index.html`
- [ ] Старий MVP-сайт видалений
