# Старий експорт з Claude Design canvas (47 МБ, дублює фото товарів,
# усе потрібне з нього вже перенесено в products.js/style.css/app.js).
Дизайн.zip

# Службові файли інструментів (Claude Code тощо), не частина сайту.
.claude/

# Вихідні PNG з вирізаним фоном — по 1–1.6 МБ кожен, ~9.6 МБ разом.
# На сайті працюють webp із тієї ж папки (у 10 разів легші, та сама
# різкість). PNG лишаються локально як джерело для перезбирання.
Без фону/*.png

# Знімок кондитерки під фоновий герой — від того варіанта відмовились
# на користь каруселі, файл лишається про запас.
Hero page.jpg
